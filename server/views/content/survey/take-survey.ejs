<div class="container mb-5 p-3 container-fluid text-center border border-dark">
<!-- TODO: Create a page that allows a survey to be filled out. -->
<!-- Important notes: This page will be passed the survey object that
        needs to be taken, which means you have access to the survey
        through item.(property), EXAMPLE: item.surveyName will return
        the name of the survey. You can reference the survey model to
        see what you have access to. This is how you will display the
        text from the questions in the survey.

        Tips to start: questions is an array.
        it stores each question as its own question object, 
        you can refernece this in the question model. 
        Each question stores its list of responses as strings.
        For each question, we need to show the question text,
        and a radio button for each of the responses in the question.
            -- Josh H -->

    <!-- Start with a form, and create a section for each question. -->
    <!-- Inside each question, list the responses as radio buttons. -->
    <!-- At the end of the form, have a submit button. -->
    <!-- NAMING NOTES: all radio button inputs REQUIRE a 'name' property,
            and they ALL must be set to 'q#Response' -->
    <!-- EX: <input type="radio" name="q1Response" .....> -->
    <form id="takeSurvey" method="POST" class="col-lg-12 col-md-10 col-sm-10 d-flex flex-column">
        <div class="mb-3">
            <h2 id="surveyName">Survey Name: <%= item.surveyName %></h2>
            <h4 id="surveyDescription">Description: <%= item.surveyDescription %></h4>
        </div>
        <!-- The amount of questions will vary based on each survey, so this needs to be dynamic -->
        <% for(let questionNumber = 0; questionNumber < item.surveyQuestions.length; questionNumber++) { %>
        <!-- Div that holds each QUESTION -->
        <div class="qdiv mb-3 p-2 border">
            <span class="mx-2 questionText"><strong>Question <%= questionNumber+1 %>:</strong> <%= questions[questionNumber].questionText %></span>
            <div class="btn-group" role="group">
                <!-- The amount of responses varies, so this needs to be dynamic as well -->
                <% for(let responseNumber = 0; responseNumber < questions[questionNumber].questionResponses.length; responseNumber++) { %>
                <input id="q<%= questionNumber+1 %>a<%= responseNumber+1 %>" type="radio" class="btn-check" value="<%= questions[questionNumber].questionResponses[responseNumber] %>" name="q<%= questionNumber+1 %>Response">
                <label for="q<%= questionNumber+1 %>a<%= responseNumber+1 %>" class="btn btn-outline-primary"><%= questions[questionNumber].questionResponses[responseNumber] %></label>
                <% } %>
            </div>
        </div>
        <% } %>
        <!-- Section for submission button -->
        <button type="submit" id="submitButton" class="btn btn-primary">Submit Survey</button>
    </form>
</div>